setuptools<81  # moviepy 1.0.3 imports pkg_resources at runtime, which newer setuptools no longer ships
flask==3.0.0
google-genai
openai==1.12.0
# openai-whisper==20231117
# ^ commented out: its setup.py imports pkg_resources during pip's isolated build step, which fails
#   on any environment with a current setuptools (pip always fetches the latest setuptools for that
#   isolated build regardless of the pin above), breaking `pip install -r requirements.txt` entirely —
#   this would fail the whole install on a fresh machine or Render build, not just the STT feature.
#   Whisper STT is optional (lazy-imported only when the user checks the STT box) — install it
#   separately with `pip install openai-whisper` in an environment that still has a pre-2025 setuptools
#   if you need that feature; the rest of the app works fully without it.
moviepy==1.0.3
apscheduler==3.10.4
python-dotenv==1.0.0
ffmpeg-python==0.2.0
Pillow==10.2.0
imageio==2.33.1
imageio-ffmpeg==0.4.9
tweepy==4.14.0
requests==2.31.0
